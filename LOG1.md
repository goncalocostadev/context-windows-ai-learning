# Learning Log --- Day 1

**Date:** 2026-07-11

## Completed Today

### Data Types
- Reviewed what data types are.
- Learned that values have types (variables can hold different types over time).
- Identified all JavaScript data types:
  - String
  - Number
  - BigInt
  - Boolean
  - Null
  - Undefined
  - Symbol
  - Object
- Understood the difference between primitive values and objects at a high level.

### Strings
- Reviewed string creation using:
  - `""`
  - `''`
  - `` ` ``
- Learned string concatenation with `+`.
- Learned template literals and string interpolation using `${}`.
- Decided **not** to study every string method yet; use MDN as a reference when needed.

### Comparisons
- Read the JavaScript.info Comparisons lesson.
- Practiced:
  - `>`
  - `<`
  - `>=`
  - `<=`
  - `==`
  - `!=`
  - `===`
  - `!==`
- Learned:
  - Prefer `===` and `!==` by default.
  - `==` and `!=` perform type coercion.
  - `===` and `!==` compare both value and type.